# ========================================
# SUPABASE CONFIGURATION
# ========================================

# DEVELOPMENT (Free Tier)
# Create at: https://app.supabase.com/new/[your-org]
SUPABASE_URL_DEV=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_ANON_KEY_DEV=eyJ[YOUR-DEV-ANON-KEY]
SUPABASE_SERVICE_KEY_DEV=eyJ[YOUR-DEV-SERVICE-KEY]  # Admin operations only

# PRODUCTION (Pro Tier - $25/month)
# Create separate project for production
SUPABASE_URL_PROD=https://[YOUR-PROD-PROJECT-REF].supabase.co
SUPABASE_ANON_KEY_PROD=eyJ[YOUR-PROD-ANON-KEY]
SUPABASE_SERVICE_KEY_PROD=eyJ[YOUR-PROD-SERVICE-KEY]  # Keep very secure!

# ========================================
# ENVIRONMENT CONTROL
# ========================================

# Switch between 'development' and 'production'
NODE_ENV=development

# Feature flag for gradual migration
USE_SUPABASE=false  # Set to true when ready to switch
SUPABASE_MIGRATION_MODE=parallel  # Options: parallel, supabase-only, legacy-only

# ========================================
# LEGACY (Current Setup - Keep During Migration)
# ========================================

# Neon Database (current)
DATABASE_URL=postgresql://neondb_owner:npg_leuHwm5rSOa7@ep-jolly-glitter-ae3liidk.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require

# Firebase (if still using)
FIREBASE_API_KEY=[your-firebase-api-key]
FIREBASE_AUTH_DOMAIN=[your-auth-domain]
FIREBASE_PROJECT_ID=[your-project-id]

# ========================================
# MIGRATION HELPERS
# ========================================

# Data sync options
SYNC_BATCH_SIZE=100
MIGRATION_DRY_RUN=true  # Test migrations without writing
ENABLE_MIGRATION_LOGS=true

# Rollback safety
LAST_SAFE_BACKUP_DATE=2024-01-15
ROLLBACK_ENABLED=true