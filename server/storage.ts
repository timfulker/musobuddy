// Main storage interface - delegates to existing core storage
export { storage } from './core/storage';

// For backward compatibility, also export the storage class
export { Storage } from './core/storage';